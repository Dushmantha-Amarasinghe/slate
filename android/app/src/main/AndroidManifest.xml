<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Reminders (Phase 3): exact-time local notifications, no data leaves the device. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Settings > Notifications & Reminders' "Battery optimization" row —
         without this declared, permission_handler's
         ignoreBatteryOptimizations.request() silently no-ops instead of
         showing the system exemption dialog. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- Voice notes (Phase 5): local mic capture only, no cloud upload. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- In-app update checker (Phase 6): the only thing that ever touches
         the network — a GitHub Releases API check and, if the user taps
         Update Now, downloading the new APK. Installing it requires the
         Android 8+ "install unknown apps" per-app grant, requested at the
         point of use (see core/update/install_handler.dart). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <application
        android:label="slate"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Flutter's default FlutterActivity treats any launch Intent's
                 `data` URI as the initial go_router location — which is
                 exactly the slate://widget/... URI HomeWidgetLaunchIntent
                 sets below, and go_router has no route registered for that
                 (it's meant to be intercepted and translated by
                 WidgetLaunchHandler instead, via the home_widget plugin's
                 own channel, not go_router's routing). Without this flag,
                 that URI also reaches go_router directly and renders its
                 "no routes for location" error screen underneath the
                 correctly-opened sheet. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Home widget (Phase 7): lets HomeWidgetLaunchIntent route a
                 widget tap (Today header, a task row, or the quick-add
                 button) back into this same Activity instance with a
                 slate://widget/... URI instead of spawning a new task. -->
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.LAUNCH"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: handles notification taps/actions (Done/Snooze)
             and re-delivers exact alarms after reboot without opening the app.
             ActionBroadcastReceiver is the one that routes Done/Snooze action-button
             taps to our Dart handler — without it those taps go nowhere. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Today widget (Phase 7): today's top N tasks + quick-add. -->
        <receiver android:name=".TodayWidgetProvider" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_widget_info"/>
        </receiver>

        <!-- Lets a widget row tap run Dart code (mark a task complete) via
             HomeWidgetBackgroundIntent without opening the app, when
             Settings > Widget's tap action is "Mark complete" — see
             core/widget/widget_background_handler.dart. -->
        <receiver android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
